pipeline {
    agent any

    stages {
        stage('Display README') {
            steps {
                echo 'Hello John from JTE Repo!'
                sh 'cat README.md'
            }
        }
    }
}
