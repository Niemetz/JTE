pipeline {
    agent any

    stages {
        stage('Display README') {
            steps {
                sh 'Hello John from JTE Repo!'
                sh 'cat README.md'
            }
        }
    }
}
